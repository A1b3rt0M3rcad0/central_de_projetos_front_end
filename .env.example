# ==============================================================================
# ARQUIVO DE CONFIGURAÇÃO DE AMBIENTE - FRONTEND
# ==============================================================================
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para .env: cp .env.example .env
# 2. Preencha o valor de VITE_API_URL conforme seu ambiente
# 3. NUNCA commite o arquivo .env no git (já está no .gitignore)
# 4. Após alterar, reinicie o servidor de desenvolvimento
#
# ==============================================================================

# ------------------------------------------------------------------------------
# URL DA API DO BACKEND
# ------------------------------------------------------------------------------
# URL completa do backend incluindo o prefixo /api/v1
#
# FORMATO: URL completa sem barra no final
#
# EXEMPLOS:
#   Desenvolvimento local:
#     VITE_API_URL=http://localhost:8000/api/v1
#   
#   Produção:
#     VITE_API_URL=https://api.example.com/api/v1
#
# IMPORTANTE: 
#   - Em produção, esta variável é OBRIGATÓRIA
#   - Se não definida em produção, a aplicação lançará erro
#   - Em desenvolvimento, se não definida, usa fallback localhost com aviso
#   - Não inclua barra no final da URL
VITE_API_URL=http://localhost:8000/api/v1
